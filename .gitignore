.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
terraform.tfvars

# Ansible Vault passphrase (the only thing teammates need to obtain out-of-band)
.vault_pass

# Cluster SSH key restored from the vault for local ssh/scp
.cluster_ssh/

kubeconfig.yaml
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
