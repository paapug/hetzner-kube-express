.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars

# Terragrunt local cache (per cluster/root.hcl). State itself lives in R2.
**/.terragrunt-cache/

# Per-developer R2 token / env files
.envrc
.env
.env.*
!.env.example

# Cluster SSH key restored locally from R2 secrets.json
.cluster_ssh/

kubeconfig.yaml
*_kubeconfig.yaml
*_kustomization_backup.yaml
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
