.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars

# Terragrunt local cache (per infra/root.hcl). State itself lives in R2.
**/.terragrunt-cache/

# Per-developer R2 token / env files
.envrc
.env
.env.*
!.env.example

# Cluster SSH key restored locally from R2 secrets.json
.cluster_ssh/

# Kubeconfig fetched from R2 (per-env, by fetch-kubeconfig.sh)
.kube/

kubeconfig.yaml
*_kubeconfig.yaml
*_kustomization_backup.yaml
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Cursor Plans
.cursor/plans/

# Python bytecode (signoz module ships dashboards_import.py)
__pycache__/
*.py[cod]